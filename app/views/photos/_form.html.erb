<%= form_for(@photo, :html => { :id => 'photo-upload-form', :class => 'dropzone' }) do |f| %>
    <%= f.file_field :image, :style => 'display: inline; opacity: 0; width: 0; height: 0;' %>
    <%= f.submit %>
<% end %>