dependencies:
  pre:
    - sudo apt-get install -t trusty/main gsl-bin libgsl0ldbl libgsl0-dev
    - sudo ldconfig
    - gsl-config --version
deployment: 
  production:
    branch: master
    heroku:
      appname: replayapp
