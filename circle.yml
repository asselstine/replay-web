dependencies:
  pre:
    - sudo apt-get install -t trusty/universe gsl-bin
    - sudo ldconfig
    - gsl-config --version
deployment: 
  production:
    branch: master
    heroku:
      appname: replayapp
